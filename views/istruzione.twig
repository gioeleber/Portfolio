{% extends 'layout/app.twig' %}

{% block content %}
    {% for lavoro in lavori %}
      {{ lavoro.renderItem("istruzione", is_first)|raw }}
      {% set is_first = false %}
    {% endfor %}
{% endblock %}
