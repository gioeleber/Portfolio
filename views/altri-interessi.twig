{% extends 'layout/app.twig' %}

{% block content %}
    {% for interesse in interessi %}
      {{ interesse.renderItem("altri-interessi",is_first)|raw }}
      {% set is_first = false %}
    {% endfor %}
{% endblock %}
